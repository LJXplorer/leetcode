```
 ** 快速排序是从全局到局部，递归语句放在最后 **
 ** 归并排序是从局部到全局，递归语句放在前面 **
```

** 递归 **

```
1）终止条件
2）本级递归（通用）需要做什么
3）返回值

注: 不要纠结每一级的细节
```

----------

### 01_快速排序 [quick_sort]

```
#include <iostream>

const int N = 100010;
int n, a[N];

void quick_sort(int a[], int l, int r) {
    if (l >= r) return;
    int x = a[l + r >> 1];
    int i = l - 1, j = r + 1;                       // 1. 为什么 i, j 取过边界
    while (i < j) {                                 // 2. 为什么外层循环判断条件不是 i <= j
        do i++; while (a[i] < x);                   // 3. 为什么内层循环用 do…while… 而不是 while
        do j--; while (a[j] > x);                   // 4. 为什么内层循环判断条件不能是 <= x 或 >= x
        if (i < j) std::swap(a[i], a[j]);
    }
    quick_sort(a, l, j), quick_sort(a, j + 1, r);   // 5.为什么递归分区是(l, j)&(j + 1, r)
}

int main() {
    std::cin >> n;
    for (int i = 0; i < n; i++) std::cin >> a[i];
    quick_sort(a, 0, n - 1);
    for (int i = 0 ; i < n; i++) std::cout << a[i] << " ";
    
    return 0;
}
```

#### 1. 为什么 i, j 取过边界？
为了用 do…while…，理由看下面的3.

#### 2. 为什么外层循环判断条件不是 i <= j ？
因为此时 j 可能取到 l - 1，导致无限递归
```
例：{o, O} => 第一次while循环后：i = r，j = l - 1，
    此时quick_sort(a, j + 1, r) => (l, r) => 会无限递归
```

#### 3. 为什么内层循环用 do…while… 而不是 while ？
因为 do…while… 可以保证 i 和 j 至少移动一位，避免卡住不动，
如果用while，那就会先判断再移动，如果元素都相等，i 和 j 移动不了，会导致死循环
那这样 ↓ 用while为什么不行？
```
while (a[i] < x) i++;
while (a[j] > x) j--;
if (i < j) swap(a[i], a[j]), i++, j--;
```
```
例：当数组为 {1, 3, 4, 2}，第一次while循环后：{1, 2, 4, 3}, i = j = 2，
    所以此时递归分区为(l, j)(j + 1, r) = (0, 2)(3, 3) => ERROR：j 在第一次 swap 之后不应该 -1
```

#### 4. 为什么内层循环判断条件不能是 <= x 或 >= x ？
如果元素都相等，i 或 j 可能会一直 +1，会导致访问过界
```
例：{1, 1}，a[i] = x = 1，符合a[i] <= x，此时 i 会一直 +1 ，导致最后 i > r
```

#### 5. 为什么递归分区是 (l, j)&(j + 1, r)？
先说结论：
```
当基准值x向 下 取整或为 a[l] 时，递归划分：(l, j)&(j + 1, r)
当基准值x向 上 取整或为 a[r] 时，递归划分：(l, i - 1)&(i, r)
万能划分：(l, i - 1)&(j + 1, r)
```
* 递归区间的选择：
    在跳出 i < j 的 while 循环时: i >= j（i&j 不是重合就是紧挨着: l...i(j)...r 或 l...ji...r）
    此时可选区间: (l, j)&(i, r), (l, j)&(j + 1, r), (l, i - 1)&(i, r), (l, i - 1)&(j + 1, r)
    可以确定的是: 
        a[l ~ j] <= x
        a[l ~ i - 1] <= x
        a[i ~ r] >= x
        a[j + 1 ~ r] >= x
    又因为 (l, j)&(i, r) 可能会出现区间重叠（当 i > j 时），重复处理导致规模无法缩小，会无限递归
    ```
    例: {1, 2} => (0, 0)&(0, 1) => (0, 0)&(0, 1) …
    ```
    所以最后可选区间: 
    (l, j)&(j + 1, r), (l, i - 1)&(i, r), (l, i - 1)&(j + 1, r)

* 基准值 x 的选择：
    ** 快排属于分治算法，需要避免无限划分：(l, r)分为(l, l)&(l, r)或者(l, r)&(r, r) **
    * 如果 x 向下取整或取左边界：
        即 i 和 j 可能取到 l，则(l, i - 1)&(i, r) => (l, l - 1)&(l, r) => 无限递归
        例：{o, O} => i = j = l => (l, l - 1)&(l, r) => 无限递归
        而(l, j)&(j + 1, r) => (l, l)&(l + 1, r), 刚好避免了无限递归
    * 如果 x 向上取整或取右边界：
        即 i 和 j 可能取到 r，则(l, j)&(j + 1, r) => (l, r)&(r + 1, r) => 无限递归
        例：{o, O} => i = j = r => (l, r)&(r + 1, r) => 无限递归
        而(l, i - 1)&(i, r) => (l, r - 1)&(r, r), 刚好避免了无限递归
    * 无论x取什么，(l, i - 1)&(j + 1, r) 都可以完美避免无限递归，因为它在递归划分的时候根本不带基准值x玩
    ```
    例：{1, 2, 3} => (l, l)&(r, r)
    ```